# 내일 작업 계획 — 2026-06-24

> W1 마무리: PoC ③ 지식 그래프 + 평가셋 초안 → **Gate 1 통과** 목표
> 선행: `ANTHROPIC_API_KEY` 필요 (현재 `backend/.env` placeholder — 실제 키 입력 먼저)

---

## 0. 사전 준비 (블로커)
- [ ] `backend/.env`에 실제 `ANTHROPIC_API_KEY` 입력 (PoC ③ LLM 추출 필수)
- [ ] `OPENAI_API_KEY`도 입력 (임베딩 — W2부터 쓰나 미리 확인)
- [ ] Docker 3서비스 기동 확인 (`docker compose ps`)

## 1. PoC ③ — 엔티티/관계 추출 → Neo4j 적재 (P0)
- [ ] `backend/app/graph/extractor.py` — 샘플 텍스트 1건 → Claude로 엔티티/관계 추출
  - LlamaIndex `SchemaLLMPathExtractor` (열린 스키마, 선택적 스키마 힌트로 환각 억제)
  - 입력: PoC1 로더 산출 청크(펌프 매뉴얼 — P-101/M-22/B-7/S-5 + 관계)
  - 기대 추출: `(P-101)-[구동]->(M-22)`, `(P-101)-[지지]->(B-7)`, `(B-7 진동초과)-[절차]->(M-22 정지)` 등
- [ ] `backend/app/graph/store.py` — Neo4j 적재
  - 노드에 출처 역링크 저장: `doc_id` / `location` / `chunk_id`
  - 그래프-벡터 양방향 ID 연결 기반 마련
- [ ] `scripts/poc3_graph.py` — 추출→적재 실행, Neo4j Browser에서 노드/엣지 1건 시각화 확인
- [ ] 환경 메모: git-bash + unstructured/torch segfault → **PowerShell 또는 백그라운드 실행**

## 2. 평가셋 초안 (P0)
- [ ] `eval/dataset.jsonl` (또는 yaml) — 펌프 매뉴얼 기반 10문항 골격
  - 멀티홉 5: 예) "베어링 B-7 진동 초과 시 가장 먼저 정지할 설비는?" (B-7→절차→M-22)
  - 단순 5: 예) "기계식 실 S-5 점검 주기는?" (표 직접 조회)
  - 각 문항: `question` / `answer` / `source(doc+location)` 필드
- [ ] 정답·출처를 샘플 문서 실제 내용에 맞춰 고정 (매주 측정 기준선)

## 3. Gate 1 체크리스트 (통과 조건)
- [ ] 3개 포맷 로딩 + 위치 메타 보존 — **이미 PoC1서 완료**
- [ ] Neo4j 노드/엣지 1건 시각화 OK — PoC ③에서 달성
- [ ] 평가셋 10문항 초안 완성
- [ ] 표 인지 청킹 1건 — **이미 PoC2서 완료**

## 4. 리스크 / 주의
- **함정 구간 W3 선행 맛보기**: 관계 추출 과소추정 단골. 완벽주의 금지 — 핵심 관계 몇 개만, 품질 낮으면 룰기반 보강.
- Claude 추출 비용↑ → 대상 청크 수 제한 + 추출 결과 캐싱.
- PoC ③는 1건 end-to-end 연결이 목표지 품질 최적화 아님.

## 5. 완료 후
- [ ] 작업 로그 `docs/작업로그/2026-06-24_*.md` 작성
- [ ] 커밋
- [ ] (Gate 1 통과 시) W2 수집 파이프라인 진입 준비

---

### 참고: 현재까지 구축된 것 (2026-06-23 기준)
- 환경: Docker 3서비스 + Python 3.12 venv + 의존성(import 13/13)
- `backend/app/loaders/` — 멀티포맷 로더 라우터 (PoC1 완료)
- `backend/app/chunking/` — 표 인지 청커 (PoC2 완료)
- `samples/` — 펌프 매뉴얼 MD/DOCX/PDF 3종
- 상세: `docs/작업로그/2026-06-23_환경구축_PoC1_PoC2.md`
