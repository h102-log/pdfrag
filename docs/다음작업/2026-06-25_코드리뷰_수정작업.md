# 다음 작업 계획 — 2026-06-25 코드리뷰 후속

> 출처: xhigh 멀티에이전트 코드리뷰 (에이전트 75개, 62건 검증 → **52건 확정**, 10건 기각)
> 대상: `backend/app` (loaders/chunking/graph/core), `scripts/poc*.py`, `eval/dataset.jsonl`
> 핵심 진단: 청킹·로더 정확성 결함 다수. 지금 샘플(.md)로만 안 터질 뿐, 실제 PDF/DOCX 들어오면 품질 붕괴.
> 라인번호: finder가 일부 틀렸고 verifier가 교정한 실제 위치 기준.

---

## 0. 즉시 (5~30분, 우리가 이미 겪은 버그)

- [ ] **#13 placeholder 키 가드 확장** — `backend/app/graph/extractor.py` `build_llm()`
  - 현재 `if not key or key.startswith("sk-xxx") or key == "":` → 프로젝트 자체 템플릿 `sk-ant-xxxx`(`.env.example`)를 **못 잡음**.
  - 6/25 작업 때 이 가드 통과 후 추출 단계서 401로 늦게 터져 시간 낭비한 그 버그.
  - 수정: `sk-ant-` 뒤 더미패턴(`x` 반복/`your-key`/길이 부족) 감지 추가. `key == ""`는 `not key`와 중복이니 정리.

- [ ] **#4 마크다운 제목 줄 보존** — `backend/app/loaders/text_loader.py` `load_markdown` (~43-51)
  - `_HEADING` 매치 시 `section`만 세팅하고 제목 텍스트는 `buf`에 안 넣음 → "3. 점검 주기" 등이 **어느 청크 본문에도 없음**.
  - `eval/dataset.jsonl`이 그 제목 문구를 `source.location`으로 쓰는데 추출/검색서 매칭 불가.
  - 수정: 제목 줄을 다음 블록 첫 줄로 포함(또는 해당 Element에 제목 프리픽스).

---

## 1. 청킹 정확성 (P0 — 품질 8할)

- [ ] **#1 긴 문장 하드분할 fallback** — `chunker.py` `_split_text` (~43-50)
  - 경계(`.!?。…`+공백/`\n{2,}`) 없는 텍스트는 *전체 경계 0개*일 때만 하드분할 → 한국어 단락/긴 CJK 런이 800자 초과 단일 청크.
  - 영향: Claude `max_tokens=2048`/`max_triplets=12` 초과 → 관계 누락 → 답 "없음".
  - 수정: 패킹 후에도 단일 조각 > max_chars면 하드 윈도우 재적용.

- [ ] **#9 overlap이 max_chars 위반** — `chunker.py` `_split_text` (~55-58)
  - flush 가드가 sentence 추가 *전*에만 체크. seed(`tail + " " + s`)는 상한 미검사 → 822 > 800 재현.
  - 수정: seed 후 길이 재검사 또는 seed를 별도 청크로.

- [ ] **#11 overlap 생자르기 → 단어중간 조각** — `chunker.py` (~55-58)
  - `cur[-overlap:]` 120자 raw 슬라이스를 다음 청크 머리에 붙임 → 음절중간 끊긴 쓰레기. ~15% 토큰 낭비.
  - 수정: 마지막 *완결 문장*을 overlap으로 사용 (이미 `sents` 있음).

- [ ] **#10 표 행패킹 `\n` 누락 계산** — `chunker.py` `_split_table` (~76-81)
  - `cur_len = sum(len(x))`이 조인 `\n` 글자 누락 → 마크다운 표도 줄수만큼 max_chars 초과.
  - 수정: 길이 계산에 `\n` 포함.

- [ ] **#12 빈 표 → 빈 청크** — `chunker.py` `_split_table` (~68)
  - `_split_table('')` → `['']` (text 경로는 빈건 버림). Neo4j 빈 source 노드 오염.
  - 수정: 빈 content 청크 생성 스킵.

## 2. 로더 정확성 (P0 — 실제 PDF/DOCX서 터짐)

- [ ] **#2 HTML 표 행분할 불가** — `chunker.py` `_split_table` (~72) + `unstructured_loader`
  - unstructured는 표를 `text_as_html`(1줄, `|` 없음)로 저장 → `splitlines()` 1줄 → 헤더감지 실패 → 통짜 1청크 (3815자 → 1조각 재현). 문서 약속한 "큰 표 행분할" 불변식 깨짐.
  - 수정: HTML 표 파서 분기 추가(BeautifulSoup 등 `<tr>` 기준) 또는 로더서 HTML→마크다운 정규화.

- [ ] **#3 PDF 표 텍스트 중복** — `backend/app/loaders/pdf_loader.py` (~32-44)
  - `table_bboxes` 모으지만 안 씀(죽은 코드). 페이지 텍스트서 표 영역 미삭제 → 표 값이 ①표Element ②페이지텍스트 두 번. 그래프 부풀고(중복 엔티티/MENTIONS) 검색 표편향.
  - 지금 안 터지는 건 샘플 PDF가 공백정렬이라 PyMuPDF가 표 인식 못 해서.
  - 수정: `table_bboxes`로 페이지 텍스트서 표 영역 clip/redact.

- [ ] **#5 DOCX 로드 crash (exit 127)** — `router.py` → `unstructured_loader`
  - `.docx`→unstructured/torch 네이티브 크래시 재현. poc1/poc2가 `pump_report.docx` 도는 순간 죽어 PDF 단계 도달 못 함.
  - 수정: W3 전 unstructured 환경 안정화(별도 프로세스/PowerShell) 또는 대체 파서. 단기: poc1/poc2 try/except로 포맷별 격리.

## 3. 설계 약점 — 멀티홉·출처 (P1, 결정 필요)

- [ ] **#6 섹션별 청킹 → 멀티홉 깨질 위험** — `chunker.py` `chunk_document` + `text_loader`
  - heading마다 청크 분리. 표(3절)·절차(4절) 딴 청크. `SchemaLLMPathExtractor`는 청크 내부만 추출 → 멀티홉(m1/m2/m3)은 **양쪽 청크가 같은 엔티티명**(`모터 M-22` vs `M-22`)을 뱉어 Neo4j 병합돼야만 성립. 이름 갈리면 노드 분리 → 답 없음.
  - 결정: (a) 엔티티 정규화/별칭 병합 (b) 청크간 관계 보강(W3 룰기반) (c) 슬라이딩 윈도우로 인접 섹션 묶기.

- [ ] **#7 출처가 `section:0`으로 뭉개짐** — `unstructured_loader._location` (~25)
  - page_number 없는 DOCX/HTML 다수가 같은 `section:0` → UI 출처 무의미. "출처 표기·다운로드 연동" 불가.
  - 수정: heading 경로/요소 인덱스 기반 고유 location 생성.

- [ ] **#8 TXT 청크 전부 `offset:0`** — `text_loader.load_text` (~58)
  - 긴 .txt N청크가 죄다 `offset:0`. base.py 약속한 offset 추적 미구현.
  - 수정: 청킹 시 누적 char offset 계산해 청크별 location 부여.

## 4. 컨벤션·정리 (P2)

- [ ] **#15 테스트 0개** — TRUST5 "85% 커버리지+특성테스트" 위반. 최소 chunker/loader 특성테스트부터.
- [ ] **#16 비번 하드코딩** — `config.py` 기본값 `graphrag123`, DB URL. 기본값 제거하고 .env 필수화.
- [ ] **#17 한국어 주석** — `base.py`/`store.py`/`chunker.py` docstring. en 정책 위반 → 영어로.
- [ ] **#18 죽은 코드** — `chunker.py` `Chunk.as_dict()` 미사용 → 제거.
- [ ] **#14 poc1/poc2 sys.path** — poc3만 부트스트랩 있음. editable 미설치 환경서 `ModuleNotFoundError`. → conftest 또는 일관된 부트스트랩.

## 5. 문서 정합성

- [ ] `docs/프로젝트_쉽게_이해하기.md` 약속 vs 코드 불일치 2곳 반영(고치거나 단서 추가):
  - "표는 절대 안 쪼개지고 자기설명적" → HTML 표 통짜(#2), PDF 표 중복(#3).
  - "멀티홉 질문 답함" → 현재 그래프선 보장 안 됨(#6).

---

## 우선순위 요약
1. **즉시**: #13(가드), #4(제목) — 작고 영향 큼
2. **P0 청킹**: #1, #9~12
3. **P0 로더**: #2, #3, #5
4. **P1 설계**: #6(멀티홉 결정), #7, #8
5. **P2 정리**: #14~18

## 메모
- 기각 10건 = 라인오류·순수 스타일(영향 없음). 무시.
- 이번 리뷰는 PoC 단계 코드 대상. W2(벡터) 진입 전 #1~#3 청킹/로더부터 잡아야 검색 품질 기반이 섬.
- 6/25 수정분(config .env 경로, extractor tool_choice 우회)은 아직 미커밋 — 본 수정과 함께 커밋.
